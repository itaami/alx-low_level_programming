#include <stdio.h>
/**
 * main - the starting of our program
 * Return: always 0 (success)
 */
int main(void)
{
	int a = 0;

	while (a <= 9)
	{
		printf("%d", a);
		a++;
	}
	printf("\n");
return (0);
}
