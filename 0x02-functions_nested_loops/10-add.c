#include "main.h"
/**
 * add -  function that adds two integers and returns the result.
 *@a: the first input that the function take
 *@b: the second input that the function take
 *Return: it's take two input and it retuens the sum
 */
int add(int a, int b)
{
	int c;

	c = (a + b);
	return (c);
}
