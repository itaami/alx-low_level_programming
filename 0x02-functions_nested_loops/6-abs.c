#include "main.h"
/**
*_abs - a  function that computes the absolute value of an integer.
*Return: always 0 (success)
*@n: the argument
*/
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (-n);
}
}
